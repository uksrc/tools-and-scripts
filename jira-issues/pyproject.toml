[project]
name = "jira-issues"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jira>=3.10.5",
    "pandas>=2.3.1",
]

[dependency-groups]
dev = [
    "pre-commit>=4.3.0",
]
